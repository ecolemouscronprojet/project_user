<h1>Liste des adresses de XXXX</h1>


<% if(typeof(addresses) === 'undefined' || addresses.length === 0) { %>
  <div class="alert alert-secondary" role="alert">
    Vous n'avez pas d'adresse !
  </div>
<% } else { %>

  <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Prénom</th>
          <th scope="col">Nom</th>
          <th scope="col">Action</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach((addresses, index) => { %>
          <tr>
            <th scope="row"><%= index+1 %></th>
            <td><%= user.firstname %></td>
            <td><%= user.lastname %></td>
            <td>
              <a href="/user/remove?id=<%= user.id %>">Supprimer</a>&nbsp;
              <a href="/user/update?id=<%= user.id %>">Modifier</a>&nbsp;
              <a href="/user/<%= user.id %>/address">Gestion adresse(s)</a>
            </td>
          </tr>
        <% }) %>
      </tbody>
  </table>

<% } %>